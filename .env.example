# WriteSpace currently requires no runtime environment variables.
# All application data is stored locally in the browser.
#
# Future client-exposed settings must use Vite's VITE_ prefix:
# VITE_EXAMPLE_SETTING=example_value